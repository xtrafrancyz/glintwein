Generate png and json files:

```
./gen.bat
```

`-kern` fonts are converted from a default ones but using `kern` table instead of `kern` feature in `GPOS` table. More:
1. https://github.com/Chlumsky/msdf-atlas-gen/issues/4
2. https://graphicdesign.stackexchange.com/questions/126786/how-to-export-font-with-kern-tables-and-not-gpos-tables-using-fontforge
